// function solution(play_time, adv_time, logs) {
//   if(play_time==='adv_time') return '00:00:00';
//   const logs_second=logs.map((v,i)=>[v.split('-').map(v2=>v2.split(':').reduce((acc,cur,idx) => acc+cur*60**(2-idx),0)),i]);
//   logs_second.sort((a,b)=>a[0][0]-b[0][0]);
//   // console.log(logs_second);
//   const play_time_second=play_time.split(':').reduce((acc,cur,idx) => acc+cur*60**(2-idx),0);
//   // console.log(play_time_second)
//   const adv_time_second=adv_time.split(':').reduce((acc,cur,idx) => acc+cur*60**(2-idx),0);
//   // console.log(adv_time_second);
//   const arr=[];
//   for(let i=0;i<logs_second.length;i++){
//       if(logs_second[i][0][0]>)
//   }
// }
